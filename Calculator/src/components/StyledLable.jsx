import React from 'react'

export const StyledLable = ({ className, htmlFor, text }) => {
    return (
        <label
            className={`form-label w-25 me-auto ${className}`}
        >

        </label>
    )
}
